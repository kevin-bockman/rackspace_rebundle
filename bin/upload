#!/usr/bin/env ruby
#
# Upload everything to a vanilla image
# prior to setup.
#
# Syntax: upload <server_address> <distro> <ssh_key>
#
# cary@rightscale.com
#
raise "Specify distro as: centos, redhat, or ubuntu" unless ARGV[1]
key=""
key="-i #{ARGV[2]}" if ARGV[2]

puts `scp #{key} -r #{ARGV[1]}/* packagediff.sh root@#{ARGV[0]}:/root/`
